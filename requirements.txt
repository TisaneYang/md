# Unified Python dependencies for the current project runtime.
#
# Scope:
# - Bench2Drive leaderboard + scenario_runner
# - MindDrive closed-loop eval/runtime
# - PilotAgent
# - RoadsideAgent
#
# Notes:
# 1. This file intentionally focuses on the current runtime/eval path.
#    Historical or old-only dependencies under `RoadsideAgent_old/` are not included.
# 2. Some heavyweight dependencies are environment-coupled and should be installed
#    separately instead of being pinned here:
#      - Python: 3.8
#      - PyTorch/CUDA: install with the wheel index that matches your CUDA stack.
#        Example used by this repo:
#          pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
#      - CARLA PythonAPI: use CARLA 0.9.15 and add its Python egg / .pth manually.
#      - detectron2: install a build matching your torch/cuda combination.
#      - flash-attn: recommended for MindDrive configs that enable flash attention,
#        but install separately with:
#          pip install flash-attn --no-build-isolation
#
# Why some versions are ranges instead of exact pins:
# - The repo already contains historical requirement files with conflicting pins
#   (for example numpy/opencv across Bench2Drive vs MindDrive).
# - The versions below are chosen to keep the current integrated runtime practical.

addict>=2.4,<3
casadi>=3.6,<4
cityscapesscripts>=2,<3
cython>=0.29,<4
dictor>=0.1,<1
diffusers==0.32.0
einops>=0.7,<1
ephem>=4,<5
gym>=0.26,<1
gymnasium>=0.29,<1
h5py>=3.8,<4
imagecorruptions>=1.1,<2
ipython>=8,<9
laspy==2.5.0
lazrs==0.5.3
loguru>=0.7,<1
lyft-dataset-sdk>=0.0.8,<1
matplotlib>=3.5,<4
motmetrics==1.1.3
networkx>=2.2,<3
nuscenes-devkit>=1.1,<2
numpy>=1.23,<1.24
numba>=0.57,<0.58
opencv-python>=4.5,<5
open3d>=0.18,<1
packaging>=23,<25
peft==0.12.0
Pillow>=9,<11
pexpect>=4,<5
prettytable>=3,<4
psutil>=5,<6
py-trees==0.8.3
pycocotools>=2.0,<3; platform_system == "Linux"
pygame>=2,<3
pyquaternion>=0.9,<1
PyYAML>=6,<7
requests>=2.28,<3
scikit-image>=0.19,<1
scikit-learn>=1.1,<2
scipy>=1.9,<2
seaborn>=0.12,<1
sentencepiece>=0.2,<1
Shapely>=1.8,<2
similaritymeasures>=1.1,<2
simple-watchdog-timer>=0.1,<1
six>=1.16,<2
stable-baselines3>=2,<3
tabulate>=0.8,<1
tensorboard>=2.10,<3
terminaltables>=3.1,<4
transformers==4.45.2
transforms3d>=0.4,<1
trimesh==2.35.39
xmlschema==1.0.18
pyzmq>=25,<27

# Tooling / test helpers used by the repo.
flake8>=6,<8
pytest>=7,<9
pytest-cov>=4,<7
pytest-runner>=6,<7
yapf==0.40.1
